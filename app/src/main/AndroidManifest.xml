<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.proyectofinal">

    //comienzo de configuracion de api de usuario

    //agregar internet para android studio
    <uses-permission android:name="android.permission.INTERNET"/>


        //se agrego la linea 13 para permitir archivo HTTP y podra conectarse a flask,
        y ya no bloqueara la API local(porque originalmente en android no se permite
        ese tipo archivo:D//

    <application
        android:allowBackup="true"

            android:networkSecurityConfig="@xml/network_security_config"

        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProyectoFinal">


        <activity android:name=".InicioActivity"/>

        <activity android:name=".RegistrarCuenta"/>

        <activity android:name=".Acciones">




            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>